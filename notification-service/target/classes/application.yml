server:
  port: 8081

spring:
  application:
    name: notification-service
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5434/notifications_db}
    username: ${DB_USERNAME:hospital}
    password: ${DB_PASSWORD:hospital}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  rabbitmq:
    host: ${RABBIT_HOST:localhost}
    port: ${RABBIT_PORT:5672}
    username: ${RABBIT_USERNAME:guest}
    password: ${RABBIT_PASSWORD:guest}

app:
  rabbitmq:
    exchange: hospital.appointments.exchange
    notification-queue: hospital.notifications.queue
    created-routing-key: appointment.created
    updated-routing-key: appointment.updated

management:
  endpoints:
    web:
      exposure:
        include: health,info
